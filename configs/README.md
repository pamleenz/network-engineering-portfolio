# Reference Configurations

These are cleaned, minimal reference configurations derived from the lab. They are not byte-for-byte running-config backups. IOS-XE defaults, certificates, HTTP settings, line configuration and unrelated platform boilerplate are intentionally omitted.

Addresses use documentation / lab ranges only.
